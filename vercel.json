{
  "github": {
    "silent": true
  },
  "buildCommand": "cd ../.. && bun install --ignore-scripts && cd apps/nextjs && SKIP_ENV_VALIDATION=true bun run build && mv .next ../../.next",
  "outputDirectory": ".next",
  "installCommand": "echo 'Skipping default install'",
  "framework": null,
  "functions": {
    "src/app/api/**": {
      "runtime": "nodejs18.x"
    }
  }
}
