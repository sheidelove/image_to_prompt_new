{
  "github": {
    "silent": true
  },
  "buildCommand": "bun install && cd apps/nextjs && contentlayer2 build && SKIP_ENV_VALIDATION=true next build",
  "outputDirectory": "apps/nextjs/.next",
  "installCommand": "echo 'Using buildCommand for install'",
  "framework": null,
  "public": false,
  "functions": {
    "apps/nextjs/src/app/api/**": {
      "runtime": "nodejs18.x"
    }
  }
}
