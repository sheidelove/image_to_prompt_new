{
  "github": {
    "silent": true
  },
  "buildCommand": "bun install --ignore-scripts && cd apps/nextjs && SKIP_ENV_VALIDATION=true bun run build",
  "outputDirectory": "apps/nextjs/.next",
  "installCommand": "echo 'Skipping default install'",
  "framework": null,
  "rootDirectory": "apps/nextjs",
  "functions": {
    "src/app/api/**": {
      "runtime": "nodejs18.x"
    }
  }
}
